---
title: "01-2: Univariate Regression: OLS Mechanics and Implementation"
format: 
  revealjs:
    html-math-method: katex
    theme: [default, ../custom.scss, ../notebook.scss]
    fontsize: 1.2em
    callout-icon: false
    scrollable: true
    echo: true
    fig-dpi: 400
    footer: "[back to course website with lecture slides](https://tmieno2.github.io/Applied-Econometrics-MS-Quarto/lectures/)"
    include-in-header:
      - ../transcript-support.html
      - ../webr-layout.html
webr:
  packages: ['AER', 'fixest', 'dplyr', 'data.table', 'ggplot2']
  cell-options:
    editor-font-scale: 0.8
    message: false
    warning: false
    # dpi must be 72: R draws the webr canvas at 72 dpi whatever we ask for,
    # so any higher value only shrinks the type. See AGENTS.md.
    dpi: 72
    fig-width: 7
    fig-height: 4.5
    out-width: 55%
filters:
  - webr
---

```{r knitr-setup}
#| include: false
#| cache: false
library(knitr)
library(AER)
library(fixest)
opts_chunk$set(echo = F)

# The R-side copy of the data and the fitted model. Both are needed by inline
# code on several slides, so they are built once here rather than part-way
# through the deck. The webr cells build their own copies in the browser.
data(HousePrices, package = "AER")
uni_reg <- feols(price ~ lotsize, data = HousePrices)
```

# Estimation of Parameters via OLS

<html><div style='float:left'></div><hr color='#EB811B' size=1px width=1000px></html>

## The data set and model

```{webr-r}
#| context: setup
# NOTE: keep every webr setup cell BELOW the first heading. A setup cell placed
# above it renders its loading widget as an untitled slide of its own.
data(HousePrices, package = "AER")

# Mirror of lecture_theme() in lectures/_lecture-theme.R — WebR cannot source
# that file. Edit it first, then run `Rscript lectures/check-lecture-theme.R`,
# which fails if this copy has drifted.
lecture_theme <- function(base_size = 16) {
  ggplot2::theme_bw(base_size = base_size) +
    ggplot2::theme(
      axis.text = ggplot2::element_text(size = base_size * 0.9),
      axis.title = ggplot2::element_text(size = base_size),
      legend.text = ggplot2::element_text(size = base_size * 0.9),
      legend.title = ggplot2::element_text(size = base_size * 0.9),
      plot.background = ggplot2::element_rect(fill = "transparent", colour = NA),
      panel.background = ggplot2::element_rect(fill = "transparent", colour = NA),
      legend.background = ggplot2::element_rect(fill = "transparent", colour = NA),
      legend.key = ggplot2::element_rect(fill = "transparent", colour = NA),
      # theme_bw's grey92 grid was chosen against a white panel and all but
      # disappears on the warm paper once the panel stops being opaque.
      panel.grid = ggplot2::element_line(colour = "grey87")
    )
}
# The WebR canvas is 7x4.5in at out-width 55% of its output track
# (lectures/_metadata.yml) — a similar pixels-per-inch to the 10in knitr canvas,
# but often narrower when the cell renders side by side, so it runs a little
# larger than the 16pt default. 30pt did not fit this canvas at all: the panel
# collapsed into the top third and the legend clipped.
theme_lecture <- lecture_theme(18)
```

::: {.panel-tabset}

### Set up

<details class="transcript">
<summary>Lecturer transcript</summary>

Let's start with a concrete data set, because everything from here on is done on real numbers. We have five hundred and forty-six houses, and for each one we know the sale price and the lot size. The model is on screen: price for house i equals beta-zero, plus beta-one times lot size for house i, plus u-i. Same shape as everything in the last lecture. And the objective is simple to state, estimate the impact of lot size on house price. Notice how much work u-i is doing. Bedrooms, bathrooms, the neighbourhood, the year of sale, all of it sits inside that one term.

</details>

**Data**

Observations of house price and lot size for `r nrow(HousePrices)` houses.

<br>

**Model**

$$price_i = \beta_0 + \beta_1 lotsize_i+u_i$$

+ $price_i$: house price (\$) of house $i$
+ $lotsize_i$: lot size of house $i$
+ $u_i$: error term (everything else) of house $i$

<br>

**Objective**

Estimate the impact of lot size on house price

### R code to get data

<details class="transcript">
<summary>Lecturer transcript</summary>

Here is the code that gets you the data, and this is a live R session running inside your browser, so you can edit it and press run. We load the AER package, which is where the HousePrices data lives, then call data to bring it into memory. The dataset has twelve columns, but the final line selects and previews only the two we use in this lecture: price and lot size. Do run it yourself rather than just reading it. The first time you press run it takes a moment, because R itself is downloading into the tab.

</details>

::: {.side-out}

```{webr-r}
#| autorun: true
 
#--- load the AER package  ---#
library(AER) # load the AER package

#--- load the HousePrices data set ---#
data(HousePrices) # load

#--- inspect the two variables used in this lecture ---#
head(HousePrices[, c("price", "lotsize")])
```

:::

### Data visualization

<details class="transcript">
<summary>Lecturer transcript</summary>

Before you estimate anything, look at your data. This is house price up the vertical axis against lot size along the horizontal. Two things to notice. First, there is clearly a positive relationship; bigger lots do sell for more. Second, and more important, look at the vertical spread at any given lot size. Take lot size four thousand and run your eye upwards: prices there range from about thirty thousand to well over a hundred thousand. That spread is the error term, made visible. Our job is to put one line through this cloud, and the scatter tells you in advance that no line will fit it well.

</details>

::: {.side-out}

```{webr-r}
#| context: output
g_hp <-
  ggplot(data = HousePrices) +
  geom_point(aes(y = price, x = lotsize), size = 0.6) +
  ylab("House Price ($)") +
  xlab("Lot Size") +
  theme_lecture

g_hp
```

:::

:::
<!--end of panel-->


## Estimation with OLS

::: {.panel-tabset}

### Rough idea

<details class="transcript">
<summary>Lecturer transcript</summary>

So here is the goal as a picture. We want to draw a line like this one through the cloud, and the slope of that line is our estimate of beta-one. The method is called ordinary least squares, OLS, and you will use it for the rest of your career. But notice I have quietly done something unjustified: I drew a line. Why this line, and not one slightly steeper, or shifted up a little? There are infinitely many lines available and I have not told you how to choose between them. The next few tabs are about answering that honestly.

</details>

+ We want to draw a line like this, the slope of which is an estimate of $\beta_1$
+ A way: Ordinary Least Squares (OLS)

::: {.side-out .tight}

```{webr-r}
#| context: output
g_hp + geom_smooth(aes(y = price, x = lotsize), method = "lm", se = F)
```

:::

### Examples

::: {.panel-tabset}

#### Ex. 1: $\widehat{\beta}_0=20000$, $\widehat{\beta}_1=7$

<details class="transcript">
<summary>Lecturer transcript</summary>

Let's take that question seriously by trying some lines out. Here is a candidate: an intercept of twenty thousand and a slope of seven. So this line claims a house with no lot at all is worth twenty thousand, and each extra unit of lot size adds seven dollars. Look at how it sits in the cloud. It is not absurd. It runs roughly through the middle of the data, and if you had to defend it in a seminar you probably could. Hold that impression, because the next tab shows a different line that also looks defensible, and they cannot both be best.

</details>


::: {.side-out .tight}

```{webr-r}
#| context: output
g_hp +
  geom_abline(intercept = 20000, slope = 7, color = "red")
```

:::


#### Ex. 2: $\widehat{\beta}_0=70000$, $\widehat{\beta}_1=3.8$

<details class="transcript">
<summary>Lecturer transcript</summary>

And here is the rival: intercept seventy thousand, slope three point eight. Higher starting point, flatter line. Flip back and forth between this tab and the previous one and you will see the problem. Both lines pass through the data. Neither is obviously wrong by eye. But they say completely different things about the world. One says an extra unit of lot size is worth seven dollars, the other says under four. If you were advising somebody on whether to buy more land, that gap matters enormously. Eyeballing is not going to settle it.

</details>

::: {.side-out .tight}

```{webr-r}
#| context: output
 
g_hp +
  geom_abline(intercept = 70000, slope = 3.8, color = "red")
```

:::

#### So,

<details class="transcript">
<summary>Lecturer transcript</summary>

Which brings us to the two questions in the box, and I want you to see that the second one is the real question. Asking which values of beta-hat-zero and beta-hat-one are best is meaningless until you say what best means. That is a decision we make, not something the data hands us. What we need is a criterion: a single number that scores any candidate line, so that comparing two lines becomes comparing two numbers. Once we have that, the whole thing turns into a minimization problem and the mathematics takes over. Defining that criterion is what the next tabs do.

</details>

:::{.callout-important title="Question"}
+ Among all the possible values of $\widehat{\beta}_0$ and $\widehat{\beta}_1$, which one is the best?
+ What criteria do we use (what does the best even mean?)
:::

:::
<!--end of panel-->

### Residuals

<details class="transcript">
<summary>Lecturer transcript</summary>

Here is the raw material for that criterion. Pick any values of beta-hat-zero and beta-hat-one, and that gives you a modeled value for every house: beta-hat-zero plus beta-hat-one times x-i. The residual is what is left over, the actual price minus the modeled price. Look at the figure, where I have taken twenty-five houses and drawn their residuals in red. Each red segment runs from a point to the line, so a long one means the line missed that house badly. Two things to keep straight. Residuals are computable, unlike the error term u, which you never see. And move the line, and every red segment changes.

</details>

For particular values of $\widehat{\beta}_0$ and $\widehat{\beta}_1$ you pick, the modeled value of $y$ for individual $i$ is $\widehat{\beta}_0 + \widehat{\beta}_1 x_i$.

Then, the residual for individual $i$ is:

$$
\widehat{u}_i =  y_i - (\widehat{\beta}_0 + \widehat{\beta}_1 x_i)
$$

That is, the residual is the observed value of the dependent variable less the modeled value. For different values of $\widehat{\beta}_0$ and $\widehat{\beta}_1$, you have a different value of residual.

The vertical red segments below <span style = "color: red;"> are </span> the residuals: each one runs from an observed point to the line.

::: {.side-out}

```{webr-r}
#| context: output
#| message: false
#--- a handful of points, so the residuals are actually visible ---#
set.seed(543)
hp_few <- HousePrices[sample(nrow(HousePrices), 25), ]
fit_few <- lm(price ~ lotsize, data = hp_few)

ggplot(data = hp_few, aes(x = lotsize, y = price)) +
  geom_segment(
    aes(xend = lotsize, yend = fitted(fit_few)),
    color = "red"
  ) +
  geom_point(size = 1.2) +
  geom_smooth(method = "lm", se = FALSE) +
  ylab("House Price ($)") +
  xlab("Lot Size") +
  theme_lecture
```

:::

### OLS

<details class="transcript">
<summary>Lecturer transcript</summary>

Now we can state the criterion, and it is the obvious one once you have seen those red segments: choose the line that makes them small overall. Specifically, square each residual, add them all up, and pick the beta-hats that make that sum as small as it can be. That is what ordinary least squares means, and the name is a literal description: least, squares. The mathematics below says exactly the same thing in symbols, minimize over beta-hat-zero and beta-hat-one the sum from i equals one to n of u-hat-i squared. That is the whole idea. Everything that follows is just carrying out the minimization.

</details>

**Idea of OLS (Ordinary Least Squares)**

Let's find the values of $\widehat{\beta}_0$ and $\widehat{\beta}_1$ that minimize the sum of the squared residuals!

<br>

**Mathematically**

Solve the following minimization problem:

$$Min_{\widehat{\beta}_0,\widehat{\beta}_1} \sum_{i=1}^n \widehat{u}_i^2, \text{where} \;\; \widehat{u}_i=y_i-(\widehat{\beta}_0+\widehat{\beta}_1 x_i)$$

### Visualization

<details class="transcript">
<summary>Lecturer transcript</summary>

Two questions on this slide, and I want you to think about them rather than take the squaring on faith. First, why square? Suppose you just added the raw residuals up. Points above the line contribute positive numbers, points below contribute negative ones, and they cancel, so a wildly wrong line could score zero. Squaring removes the sign, and it also punishes one enormous miss more than several small ones. Second, why not absolute values? You could, and that method exists; it is called least absolute deviations. Squares win mainly because they give you something you can differentiate and solve by hand, which is exactly what we do next.

</details>

```{webr-r}
#| context: output
# NOTE: "_demo" names, so this illustration does not overwrite the x, y and
# data objects the "R code: hard way" tab builds from HousePrices.
x_demo <- 200 * runif(20)
y_demo <- 100 + 0.5 * x_demo + rnorm(20) * 30
data_demo <- data.table(y = y_demo, x = x_demo)
coefs <- lm(y ~ x, data = data_demo)$coef
ggplot() +
  geom_point(data = data_demo, aes(y = y, x = x)) +
  geom_abline(intercept = coefs[1], slope = coefs[2]) +
  geom_hline(yintercept = 0) +
  geom_vline(xintercept = 0) +
  #* slope
  geom_curve(
    aes(
      x = 120, y = coefs[1] + coefs[2] * 100,
      xend = 120, yend = coefs[1] + coefs[2] * 120
    ),
    color = "red"
  ) +
  geom_segment(
    aes(
      x = 100, y = coefs[1] + coefs[2] * 100,
      xend = 130, yend = coefs[1] + coefs[2] * 100
    ),
    color = "red"
  ) +
  theme_lecture
```

**Questions**

+ Why do we square the residuals, and then sum them up together? What's gonna happen if you just sum up residuals?

+ How about taking the absolute value of residuals, and then sum them up?

### Derivation

<details class="transcript">
<summary>Lecturer transcript</summary>

Here is the calculus, and you should watch it once even if you never do it again. We are minimizing S, the sum of squared residuals, over two unknowns, so we take a partial derivative with respect to each and set both to zero. Now look at what the two first-order conditions say once you divide out the minus two. The sum of the residuals is zero, and the sum of x times the residuals is zero. Those are not assumptions we are making; they are consequences of the arithmetic, and they hold in every OLS regression you will ever run. Solve the pair and you get the formulas at the bottom.

</details>

**Minimization problem to solve**

$$Min_{\widehat{\beta}_0,\widehat{\beta}_1} \sum_{i=1}^n [y_i-(\widehat{\beta}_0+\widehat{\beta}_1 x_i)]^2$$

**Steps**

+ partial differentiation of the objective function with respect to $\widehat{\beta}_0$ and $\widehat{\beta}_1$
+ solve for $\widehat{\beta}_0$ and $\widehat{\beta}_1$

$$Min_{\widehat{\beta}_0,\widehat{\beta}_1} \sum_{i=1}^n [y_i-(\widehat{\beta}_0+\widehat{\beta}_1 x_i)]^2$$

Write the objective as $S(\widehat{\beta}_0,\widehat{\beta}_1) = \sum_{i=1}^n [y_i-(\widehat{\beta}_0+\widehat{\beta}_1 x_i)]^2$.

**FOC**

$$
\def\sumn{\sum_{i=1}^{n}}
\begin{align}
\frac{\partial S}{\partial \widehat{\beta}_0}=& -2 \sumn [y_i-(\widehat{\beta}_0+\widehat{\beta}_1 x_i)] = -2\sumn \widehat{u}_i = 0 \\\\
\frac{\partial S}{\partial \widehat{\beta}_1}=& -2 \sumn x_i\cdot [y_i-(\widehat{\beta}_0+\widehat{\beta}_1 x_i)] = -2\sumn x_i\cdot \widehat{u}_i = 0
\end{align}
$$

Dividing each by $-2$ leaves the two conditions OLS always satisfies:

$$
\def\sumn{\sum_{i=1}^{n}}
\sumn \widehat{u}_i = 0 \;\;\;\text{and}\;\;\; \sumn x_i \widehat{u}_i = 0
$$

**OLS estimators: analytical formula** 

$$
\def\sumn{\sum_{i=1}^{n}}
\begin{aligned}
  \widehat{\beta}_1 & = \frac{\sumn (x_i-\bar{x})(y_i-\bar{y})}{\sumn (x_i-\bar{x})^2},\\\\
  \widehat{\beta}_0 & = \bar{y}-\widehat{\beta}_1 \bar{x}, \\\\
  \text{where} & \;\; \bar{y} = \sumn y_i/n \;\; \text{and} \;\;\bar{x} = \sumn x_i/n
\end{aligned}
$$

### Estimators vs Estimates

<details class="transcript">
<summary>Lecturer transcript</summary>

This is a small distinction with a large payoff later, so let's be careful with it. An estimator is a rule, a formula that says what to do with whatever data you are handed. An estimate is the number that comes out when you feed that rule your particular data. The formula on screen is the estimator; six point five nine nine is the estimate, for these five hundred and forty-six houses. And here is why it matters. Hand the same estimator a different sample of houses and you get a different number out. The estimate moves around. That variability is the entire subject of the next lecture.

</details>

**Estimators**

Specific <span style = "color: red;"> rules (formula) </span> to use once you get the data

<br>

**Estimates**

Numbers you get once you plug values (your data) into the formula

<br>

**In this lecture**

+ the formula $\widehat{\beta}_1 = \frac{\sum (x_i-\bar{x})(y_i-\bar{y})}{\sum (x_i-\bar{x})^2}$ is the <span style = "color: red;"> estimator </span>
+ the number `r format(round(uni_reg$coefficients[2], 3), scientific = FALSE)`, which it returns for the `HousePrices` data, is the <span style = "color: red;"> estimate </span>

Feed the same estimator a different sample and you get a different estimate. That is the whole subject of the next lecture.
:::
<!--end of panel-->

# OLS demonstration in R

<html><div style='float:left'></div><hr color='#EB811B' size=1px width=1000px></html>

## OLS demonstration in R

::: {.panel-tabset}

### R code: hard way

<details class="transcript">
<summary>Lecturer transcript</summary>

Let's compute beta-one-hat by hand, straight from the formula, so you can see there is no magic in it. Pull the price column out as y and the lot size column as x. The numerator is the sum of x minus its mean, times y minus its mean. The denominator is the sum of x minus its mean, squared. Divide one by the other and there is your estimate. Run it and note the number, because in the next tab we get the identical number a much shorter way. I am making you do it the long way exactly once, so that the short way never feels like a black box.

</details>

**OLS Estimator Formula**

$$
\def\sumn{\sum_{i=1}^{n}}
\begin{aligned}
  \widehat{\beta}_1 & = \frac{\sumn (x_i-\bar{x})(y_i-\bar{y})}{\sumn (x_i-\bar{x})^2}\\\\
  \widehat{\beta}_0 & = \bar{y}-\widehat{\beta}_1 \bar{x}
\end{aligned}
$$

**R code**

::: {.side-out}

```{webr-r}
#| autorun: true
 
y <- HousePrices$price
x <- HousePrices$lotsize

#--- beta_1 ---#
b1_num <- sum((x - mean(x)) * (y - mean(y)))
b1_denom <- sum((x - mean(x))^2)
b1 <- b1_num / b1_denom
b1
```

:::

### R code: a better way

<details class="transcript">
<summary>Lecturer transcript</summary>

And here is the short way, which is what you will actually use. The feols function from the fixest package takes a formula, price tilde lotsize, read as price explained by lot size, and the data frame. That is the whole call. The comment shows the base R equivalent, lm, which works identically for a regression this simple; we use feols because it handles things later in the course that lm cannot. Run it and compare the slope against the number you computed by hand. They match exactly, because it is the same formula. The intercept comes along for free.

</details>

We can use the `feols()` function from the `fixest` package.

::: {.side-out}

```{webr-r}
#| autorun: true
library(fixest)

#--- run OLS on the above model ---#
# base R equivalent: lm(price ~ lotsize, data = HousePrices)
uni_reg <- feols(price ~ lotsize, data = HousePrices)

uni_reg
```

:::




### post-estimation

Lots of information is stored in the regression results (here, `uni_reg`), which is of class `list`. Each tab below pulls out one piece of it.

::: {.panel-tabset}

#### What is in there

<details class="transcript">
<summary>Lecturer transcript</summary>

What comes back from feols is not just two numbers, it is a list holding everything the regression computed, and it is worth knowing how to open it up. Applying ls to the results prints the names of everything inside. Run it and read the list. You will see coefficients, fitted values, residuals, and a number of things we have not met yet. The next four tabs pull out the ones that matter today. The general lesson is worth more than the specific names: in R, a model object is a container you can reach into, not a printout you have to squint at.

</details>

Apply `ls()` to see its elements:

::: {.side-out .tight}

```{webr-r}
#--- list the things inside the regression results ---#
ls(uni_reg)
```

:::

#### Coefficients

<details class="transcript">
<summary>Lecturer transcript</summary>

The first thing you will want is the estimated coefficients, and they live in the coefficients element. Run it and two labelled numbers come back: the intercept, and the one on lotsize. That second number, six point five nine nine, is beta-one-hat, the estimate we have been building towards for two lectures now. Read it as a sentence: one more unit of lot size is associated with about six dollars sixty more in sale price. And note the word associated. Whether you are also entitled to say caused depends on the zero conditional mean condition, which is where this course goes next.

</details>

::: {.side-out .tight}

```{webr-r}
uni_reg$coefficients
```

:::

#### Fitted values

<details class="transcript">
<summary>Lecturer transcript</summary>

Fitted values are the model's predictions at the lot sizes you actually observed: beta-hat-zero plus beta-hat-one times x-i, worked out for every house. I am printing only the first five so it stays readable. Compare these against the actual prices and you have the residuals, which is the next tab. One thing to notice is that there is one fitted value per house, so this object is exactly as long as your data set. That is what the figure back on the Residuals tab was drawing: a line's worth of predictions, one for every point in the cloud.

</details>

Predicted values at the observation points:

::: {.side-out .tight}

```{webr-r}
#* fitted values for the first 5 observation points
uni_reg$fitted.values[1:5]
```

:::

#### Residuals

<details class="transcript">
<summary>Lecturer transcript</summary>

And here are the residuals as numbers, the same quantity you saw as red segments earlier. Each one is an actual price minus the model's prediction for that house. Run it and look at the signs: some positive, some negative, which is the arithmetic guarantee we derived from the first-order condition, that they sum to zero. Then look at the magnitudes, because these are dollars, and some of them are large, tens of thousands. That is a useful reality check on a model with one explanatory variable. Lot size is genuinely informative about price, and it is nowhere near the whole story.

</details>

::: {.side-out .tight}

```{webr-r}
#* residuals for the first 5 observation points
uni_reg$residuals[1:5]
```

:::

#### `summary()`

<details class="transcript">
<summary>Lecturer transcript</summary>

If you simply want to look at a regression rather than pull pieces out of it, summary is the function. Run it and you get the standard printout: the coefficients, and beside each one some columns we have not defined yet, standard errors, t-values, p-values. Do not worry about those today. They are the machinery for deciding whether an estimate is distinguishable from zero, and lectures five and six are entirely about them. For now, read the estimate column, and notice the R-squared reported at the bottom, which is what we take apart at the end of this lecture.

</details>

You can have a nice quick summary of the regression results with the `summary()` function:

```{webr-r}
summary(uni_reg)
```

:::
<!--end of panel-->

:::
<!--end of panel-->


## Once the model is estimated

::: {.panel-tabset}

### Estimated model

<details class="transcript">
<summary>Lecturer transcript</summary>

Now that we have numbers we can write the estimated model out, and this is the object you would report in a paper. Price equals thirty-four thousand one hundred and thirty-six, plus six point five nine nine times lot size. It has a name worth knowing, the sample regression function, and it is our estimate of the population regression function, the expected value of price given lot size. Read the callout, because it holds the caveat students most often drop. Regression predicts an expected value, an average. It is not telling you what your house will sell for. It is telling you what houses like yours sell for on average.

</details>

**Model to be estimated**

$$
price = \beta_0 + \beta_1 lotsize + u
$$

<br>

**Estimated Model**

This is the estimated version of the expected value of $y$ conditional on $x$.

$$
price =  `r format(round(uni_reg$coefficients[1], 0), scientific = FALSE)` + `r format(round(uni_reg$coefficients[2], 3), scientific = FALSE)` \times lotsize
$$

This is called <span style = "color: blue;"> sample regression function (SRF) </span>, and it is an estimation of $E[price|lotsize]$, the <span style = "color: blue;"> population regression function </span>(PRF). 



:::{.callout-important}
+ OLS regression predicts the <span style = "color: red;"> expected </span> value of the dependent variable <span style = "color: blue;"> conditional on </span>the explanatory variables.

+ $\widehat{\beta}_1$ is an estimate of how a change in $x$ affects the <span style = "color: red;"> expected </span> value of $y$.
:::

### Predicted values (R)

<details class="transcript">
<summary>Lecturer transcript</summary>

This is the fitted values idea from a moment ago, met again where you would actually reach for it. The fitted values element gives you the model's prediction for each house in your data. That is useful when you want to compare predicted against actual: to plot one against the other, or to hunt down the houses your model gets most badly wrong. But notice the restriction. These are predictions at the lot sizes that happen to appear in your data. If you want a prediction at some other lot size, one nobody in your sample has, you need the next tab.

</details>

You can access the predicted values at the observed points by looking at the `fitted.values` element of the regression results.

::: {.side-out .tight}

```{webr-r}
#| autorun: true
uni_reg$fitted.values[1:5]
```

:::

### New predictions (R)

<details class="transcript">
<summary>Lecturer transcript</summary>

Predicting at new values is a two-step move, and it is worth learning properly because you will use it constantly. First, build a small data frame holding the x values you care about, here lot sizes of three thousand, twelve thousand and fifteen thousand. The column name has to match the variable name in your model exactly, or predict will not know what to do with it. Second, hand that data frame to predict along with the regression results. Out come three numbers, the expected price at each lot size. Note that fifteen thousand is near the top of the observed range; predicting far outside it is a riskier business.

</details>

To calculate the predicted value at arbitrary values of $x$,

1. create a new `data.frame` with values of $x$ of your choice.

::: {.side-out}

```{webr-r}
#| autorun: true
(
new_data <- data.frame(lotsize = c(3000, 12000, 15000))
)
```

:::

<br>

2. apply `predict()` to the `data.frame` using the regression results.

::: {.side-out .tight}

```{webr-r}
#| autorun: true
predict(uni_reg, newdata = new_data)
```

:::

### Exercise: The impact of `lotsize`

::: {.panel-tabset}

#### Problem

<details class="transcript">
<summary>Lecturer transcript</summary>

Here is a decision problem to work through yourself before you look at the solution. Your lot is three thousand. You can expand it by a thousand, and doing so costs you five thousand dollars. Should you? There is an empty code cell below for you, and you have the regression results already estimated. Read the note underneath as well, because there is a trap built into the question. I told you your current lot size is three thousand, and you do not need it. In a linear model the effect of one more unit is the same wherever you start from. That stops being true in lecture seven.

</details>

Your current lot size is 3000. You are thinking of expanding your lot by 1000 (with everything else fixed), which would cost you 5,000 USD. Should you do it? Use R to figure it out.

:::{.callout-note title="One thing to notice"}
The answer does not depend on the 3000. In a linear model the effect of one more unit of $x$ is $\widehat{\beta}_1$ wherever you start from, so the current lot size is a red herring. That stops being true the moment we allow the relationship to bend, which is lecture 07.
:::

<br>

```{webr-r}

```

#### Solution

<details class="transcript">
<summary>Lecturer transcript</summary>

Here is the answer. Pull out the coefficient on lotsize, multiply it by a thousand, because a thousand is what you are adding, and subtract the five thousand it costs. Six point five nine nine times a thousand is about six thousand six hundred; minus five thousand leaves roughly sixteen hundred dollars. Positive, so on these numbers, yes, expand. Now the honest caveat. This is an association, from a regression with a single explanatory variable and a great deal sitting in the error term. I would not stake my own five thousand on it. But the mechanics, coefficient times change, weighed against cost, are exactly right.

</details>

::: {.side-out}

```{webr-r}
#--- access the coefficient values  ---#
uni_reg$coefficients
# class(uni_reg)

#--- assess the impact ---#
uni_reg$coefficients["lotsize"] * 1000 - 5000
```

:::

:::
<!--end of panel-->

:::
<!--end of panel-->

## $R^2$: Goodness of fit

::: {.panel-tabset}

### What is it?

<details class="transcript">
<summary>Lecturer transcript</summary>

The last topic today is R-squared, and I want you to have the one-sentence version before any formulas arrive. R-squared measures how much better your model predicts the dependent variable than the laziest possible model does. And that lazy model is worth naming, because it is the benchmark the whole measure is built on: just guess the average price for every single house, ignoring lot size entirely. So R-squared answers one question. How much did knowing lot size actually buy me, over knowing nothing but the average? Everything in the next four tabs is machinery for turning that question into a number.

</details>

$R^2$ is a measure of how good your model is in predicting the dependent variable (explaining  variations in the dependent variable) <span style = "color: red;"> compared </span> to just using the average of the dependent variable as the predictor.

### Decompose $y$

<details class="transcript">
<summary>Lecturer transcript</summary>

Before the formula, here is the decomposition it rests on, and it is only algebra. Any observed y-i splits into two pieces: the fitted value, y-hat-i, and the residual, u-hat-i. Now subtract the sample average, y-bar, from both sides, and read the three bullets. On the left, y-i minus y-bar, is how far this house's price sits from the average price; that is the total deviation you are trying to account for. On the right, y-hat-i minus y-bar is the part your model explains, and u-hat-i is the part it does not. Every measure of fit compares those two right-hand pieces.

</details>

You can decompose observed value of $y$ into two parts: fitted value and residual

$$
y_i=\widehat{y}_i +\widehat{u}_i, \;\;\text{where}\;\; \widehat{y}_i = \widehat{\beta}_0+\widehat{\beta}_1 x_i
$$

now, subtracting $\bar{y}$ (sample average of $y$),

$$
y_i-\bar{y}=\widehat{y}_i-\bar{y}+\widehat{u}_i 
$$

+ $y_i-\bar{y}$: how far away the actual value of $y$ for $i$th observation from the sample average $\bar{y}$ is (actual deviation from the mean)
+ $\widehat{y_i}-\bar{y}$: how far away the predicted value of $y$ for $i$th observation from the sample average $\bar{y}$ is (explained deviation from the mean)
+ $\widehat{u_i}$: the residual for $i$th observation

### Visualization

<details class="transcript">
<summary>Lecturer transcript</summary>

Here is that split drawn for one real house, so the algebra has a picture attached to it. The red horizontal line is y-bar, the average price. The blue line is the fitted model. Now find the black dot over on the right: that is one house. The green arrow runs from the average up to the fitted line, and that is the explained part, what knowing this house's lot size buys you. The purple arrow runs from the fitted line to where the house actually sold, and that is the residual, what the model missed. Green plus purple is the total deviation.

</details>

The three pieces, drawn for a <span style = "color: blue;"> single </span> observation. The red line is $\bar{y}$; the blue line is the fitted model.

```{webr-r}
#| context: output
#--- pick the house where BOTH pieces are large, so neither arrow is a stub ---#
fit_all <- lm(price ~ lotsize, data = HousePrices)
y_bar <- mean(HousePrices$price)
i <- which.max(pmin(abs(fitted(fit_all) - y_bar), abs(resid(fit_all))))
x_i <- HousePrices$lotsize[i]
y_i <- HousePrices$price[i]
yhat_i <- fitted(fit_all)[i]

g_hp +
  geom_smooth(
    aes(y = price, x = lotsize),
    method = "lm", se = F, linewidth = 0.8
  ) +
  geom_hline(yintercept = y_bar, color = "red") +
  #--- explained deviation: y_bar up to the fitted value ---#
  annotate("segment",
    x = x_i, xend = x_i, y = y_bar, yend = yhat_i,
    color = "darkgreen", linewidth = 0.8,
    arrow = arrow(length = unit(0.015, "npc"), ends = "both")
  ) +
  #--- curved leaders connect both interval endpoints to its label ---#
  annotate("curve",
    x = x_i, y = y_bar,
    xend = x_i - 1100, yend = (y_bar + yhat_i) / 2 - 7000,
    curvature = -0.25, color = "darkgreen", linewidth = 0.7
  ) +
  annotate("curve",
    x = x_i, y = yhat_i,
    xend = x_i - 1100, yend = (y_bar + yhat_i) / 2 + 7000,
    curvature = 0.25, color = "darkgreen", linewidth = 0.7
  ) +
  #--- residual: fitted value up to the observed point ---#
  annotate("segment",
    x = x_i, xend = x_i, y = yhat_i, yend = y_i,
    color = "purple", linewidth = 0.8,
    arrow = arrow(length = unit(0.015, "npc"), ends = "both")
  ) +
  annotate("point", x = x_i, y = y_i, size = 3.2) +
  annotate("text",
    x = x_i - 1100, y = (y_bar + yhat_i) / 2,
    label = "hat(y)[i] - bar(y)", parse = TRUE, color = "darkgreen",
    size = 4.2, hjust = 0.5
  ) +
  annotate("text",
    x = x_i + 350, y = (yhat_i + y_i) / 2,
    label = "hat(u)[i]", parse = TRUE, color = "purple",
    size = 4.2, hjust = 0
  ) +
  annotate("text",
    x = 3000, y = y_bar * 1.12, label = "bar(y)",
    parse = TRUE, color = "red", size = 3
  )
```

+ green + purple together make up $y_i-\bar{y}$, the actual deviation from the mean
+ green alone, $\widehat{y}_i-\bar{y}$, is the part the model <span style = "color: blue;"> explains </span>
+ purple alone, $\widehat{u}_i$, is the part it does <span style = "color: red;"> not </span>

### $R^2$ components

<details class="transcript">
<summary>Lecturer transcript</summary>

Three sums, and the names are more confusing than the mathematics, so take them slowly. Total sum of squares, SST, adds up the squared total deviations: how much price varies overall. Explained sum of squares, SSE, adds up the squared explained parts, the green arrows. Residual sum of squares, SSR, adds up the squared residuals, the purple arrows, and you have met this one already, because it is exactly the quantity OLS was chosen to minimize. One warning: some textbooks swap the letters SSE and SSR completely, so read the definition rather than trusting the acronym.

</details>

**total sum of squares (SST)** 

$$
SST\equiv \sum_{i=1}^{n}(y_i-\bar{y})^2 
$$

**explained sum of squares (SSE)**
$$
SSE\equiv \sum_{i=1}^{n}(\widehat{y}_i-\bar{y})^2 
$$

**residual sum of squares (SSR)**
$$
SSR\equiv \sum_{i=1}^{n}\widehat{u}_i^2 
$$

### Definition of $R^2$

<details class="transcript">
<summary>Lecturer transcript</summary>

Now the definition. R-squared is one minus SSR over SST. Where that comes from is the three lines below: total equals explained plus residual, rearrange, divide through by SST. So R-squared is equally the explained share, SSE over SST, and it sits between zero and one as long as your model includes an intercept. And here is the number for our regression, computed just below: about zero point two nine. Lot size accounts for roughly twenty-nine percent of the variation in house price. The other seventy-one percent is everything in u, bedrooms, location, condition, the year it sold.

</details>

:::{.callout-note title="Definition"}
$R^2 = 1 - SSR/SST$
:::

<br>

**Where did it come from?**

```{=tex}
\begin{align}
& SST = SSE + SSR  \\
\Rightarrow & SSE = SST - SSR \\
\Rightarrow & SSE/SST = 1 - SSR/SST = R^2\\
\end{align}
```

The value of $R^2$ always lies between $0$ and $1$ as long as an intercept is included in the econometric model.

<br>

**What does it measure?**

$R^2$ is a measure of how much improvement <span style = "color: red;"> in predicting the dependent variable </span> you've made by including independent variable(s) $(y=\beta_0+\beta_1 x+u)$ compared to when simply using the mean of dependent variable as the predictor $(y=\beta_0+u)$.

<br>

**For our model**

::: {.side-out .tight}

```{webr-r}
#| autorun: true
#--- R-squared of the house price regression ---#
r2(uni_reg, "r2")
```

:::

That is how much of the variation in house price lot size alone accounts for. The rest is everything in $u$ &mdash; bedrooms, location, condition, the year it sold.

### Caveat

<details class="transcript">
<summary>Lecturer transcript</summary>

Now the warning, and it is the most important thing on this slide. R-squared tells you nothing about whether you have estimated beta-one well. Read the two blocks. We can always check how well a model predicts the dependent variable, because we observe the dependent variable. We can never check how well we estimated beta-one, because nobody ever shows us the true beta-one. So we are in the awkward position of being able to measure the thing we do not much care about, and unable to measure the thing we do. That is exactly why the next topic is assumptions.

</details>

**Important**

+ $R^2$ tells <span style = "color: red;"> nothing </span> about how well you have estimated the causal ceteris paribus impact of $x$ on $y$ $(\beta_1)$.
+ As economists, we typically do not care how well we can <span style = "color: blue;"> predict </span> the dependent variable; we care how well we have <span style = "color: blue;"> estimated </span> $\beta_1$. (We predict outcomes; we estimate parameters.)

**Problem**

+ While we observe the dependent variable (otherwise you cannot run regression), we cannot observe $\beta_1$. 
+ So, we get to check how good estimated models are in predicting the dependent variable (which we do not care about), but we can <span style = "color: red;"> never </span> test whether they have estimated $\beta_1$ well.
+ This means that we need to carefully examine whether the <span style = "color: red;"> assumptions </span> necessary for good estimation of $\beta_1$ are satisfied (next topic).

:::
<!--end of panel-->
